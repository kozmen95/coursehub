<% content_for :title, "Courses" %>

<div class="w-full">
  <div class="flex justify-between items-center mb-8">
    <h1 class="font-extrabold text-3xl text-slate-100">📚 Courses</h1>
    <% if user_signed_in? && (current_user.instructor? || current_user.admin?) %>
      <%= link_to "➕ New course",
        new_course_path,
        class: "rounded-lg px-4 py-2 bg-sky-600 hover:bg-sky-500 text-white font-medium shadow transition" %>
    <% end %>

  </div>

  <% if @courses.any? %>
    <div id="courses" class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      <% @courses.each do |course| %>
        <%= render partial: "course", locals: { course: course } %>
      <% end %>
    </div>
  <% else %>
    <p class="text-center my-10 text-slate-400">No courses found.</p>
  <% end %>
</div>
