<div class="bg-slate-800 border border-slate-700 rounded-xl shadow-lg p-6 w-full text-white">
  <h2 class="text-2xl font-bold mb-6">📖 <%= @lesson.title %></h2>

  <% if @lesson.date.present? %>
    <p class="mb-2"><strong>📅 Data:</strong> <%= @lesson.date.strftime("%Y-%m-%d") %></p>
  <% end %>

  <p class="mb-2"><strong>⏰ Start:</strong> <%= @lesson.starts_at&.strftime("%Y-%m-%d %H:%M") || "—" %></p>
  <p class="mb-2"><strong>⏰ Koniec:</strong> <%= @lesson.ends_at&.strftime("%Y-%m-%d %H:%M") || "—" %></p>
  <p class="mb-6"><strong>📍 Miejsce:</strong> <%= @lesson.location.presence || "—" %></p>

  <% if @lesson.materials.attached? %>
    <h3 class="text-lg font-semibold mt-6 mb-3">📂 Materiały</h3>
    <ul class="space-y-2">
      <% @lesson.materials.each do |file| %>
        <li>
          <%= link_to file.filename.to_s, url_for(file),
              class: "inline-block px-3 py-1 rounded bg-slate-700 hover:bg-slate-600 text-sm" %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="text-slate-400 italic">Brak materiałów do tej lekcji</p>
  <% end %>

  <div class="mt-8">
    <%= link_to "⬅️ Wróć do listy", course_lessons_path(@course),
        class: "text-indigo-400 hover:text-indigo-300 font-semibold" %>
  </div>
</div>
